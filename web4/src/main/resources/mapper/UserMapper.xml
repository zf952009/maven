<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="z.f.mapper.UseerMapper">
    <sql id="fileds"></sql>
    <select id="getUserAll" resultType="z.f.bean.UserText">
        select
    </select>
</mapper>